export interface Cartinterface {
    in: {
        id: number,
        product_Id: number,
        quantity: number,
        price: number,
        user_Id : number,
        is_Active: boolean
      }
}
